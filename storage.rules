rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only the owner of the session can upload or read their audio files
    match /users/{userId}/sessions/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}