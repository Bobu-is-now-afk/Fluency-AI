
/**
 * FIREBASE REMOVED
 * This project has been migrated to a 100% Client-Side Architecture.
 * Local persistence is handled by services/storage.ts (IndexedDB).
 */
export const db = null;
export const auth = null;
export const storage = null;
